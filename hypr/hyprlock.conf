general {
    disable_loading_bar = true
    hide_cursor = false
}


background {
    monitor =
    path = /home/mist/.config/wallpapers/w.png

    color = rgba(25, 20, 20, 1.0)

    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_passes = 1 # 0 disables blurring
    blur_size = 7
    # noise = 0.0117
    # contrast = 0.8916
    # brightness = 0.8172
    # vibrancy = 0.1696
    # vibrancy_darkness = 0.0
}

# shape {
#     monitor =
#     size = 360, 60
#     color = rgba(17, 17, 17, 1.0)
#     rounding = -1
#     border_size = 8
#     border_color = rgba(0, 207, 230, 1.0)
#     rotate = 0
#     xray = false # if true, make a "hole" in the background (rectangle of specified size, no rotation)

#     halign = center
#     valign = center
# }

input-field {
    monitor =
    size = 200, 200
    font_size = 10000
    font_family = JetBrains Mono Nerd Font Mono
    outline_thickness = 5
    dots_size = 0.1 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.0 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    outer_color = rgba(255, 255, 255, 0.0)
    inner_color = rgba(255, 255, 255, 0.1)
    
    fade_on_empty = true
    fade_timeout = 1000 
    placeholder_text =  # Text rendered in the input box when it's empty.
    hide_input = true
    rounding = -1 # -1 means complete rounding (circle/oval)
    check_color = rgb(204, 136, 34)
    fail_color = rgb(204, 34, 34) # if authentication failed, changes outer_color and fail message color
    fail_text =  # can be set to empty
    # fail_transition = 300 # transition time in ms between normal outer_color and fail_color
    capslock_color = rgb(255, 0, 0)
    # numlock_color = rgba(0, 0, 0, 0)
    bothlock_color = -1 # when both locks are active. -1 means don't change outer color (same for above)
    invert_numlock = true # change color if numlock is off
    swap_font_color = false # see below

    halign = center
    valign = center
}


# TIME
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H:%M:%S")"
    color = rgba(255, 255, 255, 1)
    font_size = 60
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = 0, -300
    halign = center
    valign = top
        shadow_passes = 1 
}
label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%d-%m-%Y")"
    position = 0, 100
    color = rgba(255, 255, 255, 1)
    font_size = 30
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    halign = center
    valign = center
        shadow_passes = 1 
}


label {
    monitor =
    text = $USER
    position = -30, 10
    font_family = JetBrains Mono Nerd Font Mono
    halign = right
    valign = bottom
}


# KEYBOARD LAYOUT
label {
    monitor =
    text = 󰌌 
    # color = $text
    font_size = 20
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    rotate = 0 # degrees, counter-clockwise
    position = 10, 1
    halign = left
    valign = bottom
    shadow_passes = 2
}

label {
    monitor =
    text = $LAYOUT[en,ru]
    font_size = 10 
    font_family = JetBrains Mono Nerd Font Mono
    rotate = 0 # degrees, counter-clockwise

    position = 30, 10
    halign = left
    valign = bottom
    shadow_passes = 2
}


# CURRENT SONG
label {
    monitor =
    text = cmd[update:1000] echo "$($HOME/.config/scripts/song.sh)"
    # color = $foreground
    color = rgba(255, 255, 255, 1)
    font_size = 18
    font_family = JetBrainsMono
    position = 0, 70 
    halign = center
    valign = bottom
    reload_time = 1
}

image {
    monitor =
    path = echo "$($HOME/.config/scripts/simageSong.sh)"
    size = 70
    border_size = 0
    position = 0, 20
    reload_time = 1 # seconds between reloading, 0 to reload with SIGUSR2
    reload_cmd = "$HOME/.config/scripts/imageSong.sh" # command to get new path. if empty, old path will be used. don't run "follow" commands like tail -F
    halign = center
    valign = bottom
    shadow_passes = 1
}

